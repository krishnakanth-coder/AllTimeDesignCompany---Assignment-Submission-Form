/** @format */

import React from 'react';
import classes from './styles.module.css';

const Header = () => {
  return (
    <div className={classes.headerEl}>
      <h1 className={classes.mainheading}>REACT JS TEST.zip</h1>
      <h4 className={classes.subheading}>5 items</h4>
    </div>
  );
};

export default Header;
